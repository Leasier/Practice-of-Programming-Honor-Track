（近似）非零元素较少的数据在现实中是很常见的，而对高维数据而言，稀疏性使得维度灾难往往并不会发生，因此有必要研究处理稀疏性的方式。

传统地，往往在“把数据读进来”之后我们才会利用稀疏性（如使用相应的高效算法、进行低秩近似等）；但数据本身蕴含的噪声和冗余事实上并不需要考虑，那能否将数据用一种“压缩”的方式将其提取出来？

### 压缩感知 (compressive sensing)

#### 介绍与设定

假设“自然”生成了未知信号 $z \in \mathbb{R}^n$，给出 $m$ 个线性测量子 $a_1, \cdots, a_m \in \mathbb{R}^n$，通过某种硬件我们可以获知 $b_1 = \langle z, a_1 \rangle, \cdots, b_m = \langle z, a_m \rangle$。欲在 $m \ll n$ 的情况下从 $b_1, \cdots, b_m$ 恢复 $z$。

将 $a_1, \cdots, a_m$ 作为行向量，堆叠出 $A \in M_{m \times n}(\mathbb{R})$；将 $b_1, \cdots, b_m$ 堆叠为列向量 $b \in \mathbb{R}^m$。则观测结果即 $Az = b$，欲设计 $A$ 以恢复 $z$。

这显然等价于解线性方程组，但由 $m < n$ 可知其**欠定**，即没有唯一解。但如果额外要求 $z$ “充分稀疏”，我们能否找到一个 $z$ 呢？

我们这样衡量稀疏性：称 $z$ 是 $k$-稀疏的，若 $z$ 中至多有 $k$ 个非零分量。一般假定 $k \ll n$。

#### 压缩感知定理

设 $A \in M_{m \times n}(\mathbb{R})$，其中 $m = O \left( k \log \dfrac{n}{k} \right)$，且 $A$ 的每个分量服从 $\mathcal{N}(0, 1)$。则 $\forall z \in \mathbb{R}^n$ 是 $k$-稀疏的，可从观测 $b = Az$ 中精确恢复 $z$。

------

下面讨论给定一个满足上述条件的 $A$，如何恢复 $z$。

自然的思路是找非零分量最少的向量 $x$，即：

$$
\arg \min_{Ax = b} ||x||_0
$$

这里 $|| \cdot ||_0$ 即 $0$-范数，表示非零分量的数量。

但事实上 $0$-范数最小化问题是 NP-Hard 的，考虑将其换成一种不 NP-Hard、但又与之“充分接近”的目标—— $1$-范数。之所以说其与目标充分接近，是因为下面的定理成立：

- 设 $A \in M_{m \times n}(\mathbb{R})$，其中 $m = O \left( k \log \dfrac{n}{k} \right)$，且 $A$ 的每个分量服从 $\mathcal{N}(0, 1)$。则以高概率地，$\forall z \in \mathbb{R}^n$ 是 $k$-稀疏的，下面问题的唯一解恰为 $z$：

$$
\arg \min_{Ax = b} ||x||_1
$$

#### $1$-范数最小化问题的求解：线性规划 (linear programming, LP)

线性规划处理的问题形如：

- 变量：$x_1, \cdots, x_n \in \mathbb{R}$。
- 约束：线性约束 $\displaystyle\sum_{j = 1}^n a_{i, j} x_j \leq b_i$。
- 目标函数：线性函数 $\min \displaystyle\sum_{j = 1}^n c_j x_j$。

作为一个历史悠久的问题，存在最坏情况下多项式时间复杂度的算法（椭球法），也存在各自实践中高效的求解器，如 [GLPK](https://www.gnu.org/software/glpk/)、[lp_solve](https://lpsolve.sourceforge.net/) 等。

回到 $1$-范数最小化问题，考虑添加辅助变量 $y_1, \cdots, y_n$ 来刻画绝对值，即将问题改写为：

$$
\begin{aligned}
\min \quad & \sum_{i = 1}^n y_i \\
\text{s.t. } \quad & Ax = b \\
& y_i - x_i \geq 0, & \forall 1 \leq i \leq n \\
& y_i  + x_i \geq 0, & \forall 1 \leq i \leq n
\end{aligned}
$$

若 $y_i > |x_i|$ 则可以令 $y_i = |x_i|$，故给出的即为最优解。

对于输入有噪声的情况，将等号的限制放宽到允许误差即可。

### 基于稀疏恢复 (sparse recovery) 的数据流算法

#### 稀疏恢复算法：概念

给定参数 $k$，稀疏恢复算法将能以高概率判定数据流的频数向量 $x$ 是否是 $k$-稀疏的；若回答为是，则将能把频数向量中非零元素的下标完全恢复出来。

存在一个以高概率成功的、单次数据流操作需要 $O(\log n)$ 时间、总共需要 $O(k \log n)$ 空间的稀疏恢复算法，其中 $n$ 描述了频数向量的下标范围 $\{1, \cdots, n\}$。这里假定 $k \ll n$。

在介绍算法流程之前，下面先来介绍一个例子。

#### 例子：近似直径的数据流算法

在第五讲中，我们介绍了线性时间求解 $(1 + \epsilon)$-近似点集直径的算法，现在我们尝试设计一个**数据流**版本的算法。

此处的“数据流”具体指：

- 给出插入、删除一定范围内的点的操作序列，只支持**单次、顺序**地访问。
- 在输入完毕后，在当时的数据集上进行某些查询操作。

在离线版本的算法中，我们需要先能够 $O(1)$-近似直径，但这难以在允许删除操作的数据流中做到。

考虑倍增“猜测”直径的值：$\forall 0 \leq i \leq O(\log_2 \Delta)$，维护格点边长为 $2^i$ 对应的 $O(\epsilon^{-d})$-稀疏恢复结构 $S_i$，插入 / 删除时将当前点从每个 $S_i$ 中插入删除，找到 $i$ 最小且回答是的 $S_i$，获取恢复的点集并将该点集的直径作为结果返回。

单次数据流操作的时间复杂度为 $O(d^2 \log \Delta \log d \Delta)$，总空间复杂度为 $O(d^2 \epsilon^{-d} \log \Delta \log d \Delta)$。

#### 稀疏恢复算法：流程与实现

当 $k = 1$，可以维护 $c_1 = \displaystyle\sum_{i = 1}^n x_i, c_2 = \sum_{i = 1}^n ix_i$，若 $c_1 = 0$ 则回答是；若 $c_1 > 0$，当只数据流中出现过一个不同元素，其编号为 $i_0 = \dfrac{c_2}{c_1}$。

接下来考虑如何检测数据流中是否出现过多个不同元素：

- 取 $q = \text{poly}(n)$ 为素数，随机取 $r \in \{0, \cdots, q - 1\}$，额外维护 $c_3 = \left( \displaystyle\sum_{i = 1}^n r^i x_i \right) \bmod q$。
- 若 $c_3 = r^{i_0} c_1 \bmod q$ 则回答是，否则回答否。

由于关于 $r$ 的 $\leq n$ 次多项式 $\displaystyle\sum_{i = 1}^n r^i x_i - r^{i_0} c_1 \in F[\mathbb{F}_q]$ 至多只有 $n$ 个零点，此时误判概率不超过 $\dfrac{n}{q} = O \left( \dfrac{1}{\text{poly}(n)} \right)$。

- 为提高效率起见，可以将此处的快速幂换成随机哈希。

------

接下来尝试推广到 $k > 1$ 的情形。首先假设数据集 $V$ 就是 $k$-稀疏的。

构造随机哈希 $h : [n] \to [2k]$，则 $\forall p \in V$，冲突概率可以估计为：

$$
\begin{aligned}
\Pr[\exists p \neq q \in V, h(p) = h(q)] &= 1 - \Pr[\forall p \neq q \in V, h(p) \neq h(q)] \\
&\leq 1 - \left( 1 - \frac{1}{2k} \right)^{k - 1} \\
&\leq \frac{1}{2}
\end{aligned}
$$

接下来取 $T$ 个独立的随机哈希 $\{h^{(i)}\}_{i = 1}^T$，$\forall 1 \leq i \leq T, 1 \leq j \leq 2k$，维护一个上面介绍的 $1$-稀疏恢复结构 $S_{i, j}$，我们期待**每一个**最终出现的 $p \in V$ 都能不冲突地出现在某个 $S_{i, j}$ 中，这样就可以直接找出所有 $1$-稀疏的 $S_{i, j}$，将其恢复出的结果（**元素和频数**）的并集作为答案。

为使**每一个** $p$ 都能以高概率实现“至少在一个哈希中不冲突”，由 Union Bound 要求 $k \left( \dfrac{1}{2} \right)^T \leq \delta$，取 $T = O \left( \log \dfrac{k}{\delta} \right)$ 即可。

最后，我们还需要检测数据集是否存在多于 $k$ 个不同元素：

- 如果恢复出的元素数量 $> k$，则必然不是 $k$-稀疏的。
- 否则，计算这些元素的出现次数之和，检查其是否与数据流给出的元素数量一致即可。

取 $\delta = \dfrac{1}{\text{poly}(n)}$，可得 $T = O(\log n)$。假设单次哈希操作的时间复杂度为 $O(1)$，则单次数据流操作的时间复杂度为 $O(\log n)$，总空间复杂度为 $O(k \log n)$。

#### Linear Sketch

称一个数据流算法为 Linear Sketch，若可将其视为以下形式：

- 设 $x$ 为频数向量，则算法在数据流下维护的是 $\mathcal{A} x$，其中 $\mathcal{A}$ 是一个线性的操作，且“可以认为” $\mathcal{A} x$ 的空间开销与数据流长度无关（比如 $\text{poly} \log$ 等远低于线性的相关性）。
- 回答询问时，算法仅利用了 $\mathcal{A} x$ 而不需要 $x$。

例如上面介绍的稀疏恢复算法就是 Linear Sketch，因为其本质上维护的是若干求和。

Linear Sketch 的优点在于**可合并**、**可作差**，这使其能够用于并行、分布式算法中。

### 基于随机采样的数据流算法

当数据流具有较强的稀疏性，上面的稀疏恢复算法可以有效地全面恢复出数据；但当数据流更加稠密，我们则转向**通过采样统计数据流上的信息**。

形式化地，设 $\text{supp}(x)$ 为频数向量中非零下标组成的集合，我们期待取得其上的一个均匀采样 $p$，使得：

$$
\forall q \in \text{supp}(x), \Pr[p = q] = \dfrac{1}{||x||_0}
$$

#### 数据流随机采样：思路与实现

如果已知 $||x||_0$ 的值，则可以将 $\text{supp}(x)$ 中的所有元素以 $\dfrac{1}{||x||_0}$ 的概率采样，采样出来的元素以常数概率只有 $1$ 个，此时即可调用 $1$-稀疏恢复取得。

与稀疏恢复算法的思路一致，我们倍增地“猜测” $\text{supp}(x)$ 的大小为 $2^i$，维护 $1$-稀疏恢复结构 $S_i$，并设有随机哈希 $h^{(i)} : [n] \to \{0, 1\}$，使得 $\forall p \in [n], \Pr[h(p) = 1] = \dfrac{1}{2^i}$。

在插入 / 删除元素 $p \in [n]$ 时，若 $h^{(i)}(p) = 1$ 则将 $p$ 插入 $S_i$ / 从 $S_i$ 中删除。

在采样时，取 $1$-稀疏的 $S_i$，返回恢复出来的元素即可。

上面的过程具有常数成功概率。通过独立重复运行 $O \left( \log \dfrac{1}{\delta} \right)$ 个上述结构，可将成功概率提升到任意的 $\geq 1 - \delta$。

与稀疏恢复一致，这也是一个 Linear Sketch。

#### 应用：实现稀疏恢复算法

考虑独立运行 $O(n \log n)$ 个上述随机采样算法，将其所得采样结果取并，则当数据流的确是 $k$-稀疏的，可以以 $\dfrac{1}{\text{poly}(n)}$ 的错误概率完成稀疏恢复。

#### 应用：数据流连通分量

首先考虑一个简单的离线做法：

- 设输入的无向图为 $G = (V, E)$，初始将每个点 $v_i \in V$ 作为一个独立的连通分量 $S_i = \{v_i\}$。
- 对每个连通分量 $S_i$ 找一条跨越边（即一端在 $S_i$ 中、一端在 $S_i$ 外），若存在则加入，完成后更新连通分量信息。
- 若不再有新边加入，停止并输出结果。

由于每轮中还需更新的连通分量数量至少减半，时间复杂度为 $O((|V| + |E|) \log |V|)$。

现在考虑数据流的情形，即每次加入 / 删除一条边，要求**不依赖于 $|E|$ 地精确计算**无向图的连通分量。我们允许至多 $\tilde{O}(|V|)$ 的空间占用。

考虑如何使用较少的空间找到连通分量的跨越边。下面是一种巧用频数向量的方式：

- $\forall v \in V$，定义频数向量 $x_v \in \mathbb{Z}^{|V|^2}$：

$$
x_v(u, v) = \begin{cases}
1, &\quad (u, v) \in E, u \leq v \\
-1, &\quad (u, v) \in E, u > v \\
0, &\quad \text{otherwise}
\end{cases}
$$

- 设 $S \subset V$，则在 $x_S \triangleq \displaystyle\sum_{v \in S} x_v$ 中，内部边对应的节点正负相消，只有跨越边对应的位置有值。

考虑对每个点 $v \in V$，维护频数向量为 $x_v$ 的采样结构 $\mathscr{L}_v$，每插入 / 删除一条边时更新对应的采样结构，查询时计算 $\mathscr{L}_{S_i} = \displaystyle\sum_{v \in S_i} \mathscr{L}_v$，然后从其上生成一个采样，即可得到 $S_i$ 的一条跨越边。

需要注意的是，为确保采样的均匀性与独立性，每一轮循环中需要一组新的 $\{\mathscr{L}_v\}_{v \in V}$，故维护一共 $O(|V| \log |V|)$ 个独立的随机采样结构即可。

#### 应用：数据流 $(1 + \epsilon)$-近似 MST

将上面的离线算法的“任取一条跨越边”改成“取一条边权最小的边”即得求解 MST 的 Borůvka 算法。

考虑将**连续**的边权改为**离散**的：考虑到 $\epsilon$ 的容错，不妨设边权均 $> 0$，则将边权上取整到 $1 + \epsilon$ 的方幂，此时共有 $\log_{1 + \epsilon} w = O \left( \dfrac{w}{\epsilon} \right)$ 种边权，对每一种边权分别维护上述采样结构，找跨越边时求从小到大枚举 $1 + \epsilon$ 的方幂，取最小且存在相应跨越边者即可。
