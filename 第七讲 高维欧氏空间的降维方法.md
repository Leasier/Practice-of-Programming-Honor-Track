### 针对距离的降维：Johnson-Lindenstrauss (JL) 变换

给定相对误差限制 $\epsilon$，JL 变换指映射 $f : \mathbb{R}^d \to \mathbb{R}^m$，使得下式以大概率成立：

$$
\forall x, y \in P, \|f(x) - f(y)\| \in (1 \pm \epsilon) \cdot \|x - y\|
$$

其中 $m = O \left( \dfrac{1}{\epsilon^2} \log n \log \dfrac{1}{\delta} \right)$，其中 $\delta$ 为失败率限制。

------

下面来构造这样的 $f$，总体思路如下：

- (1) 首先注意到，只需满足：

$$
\forall x, y \in P, \|f(x) - f(y)\|^2 \in (1 \pm \epsilon) \cdot \|x - y\|^2
$$

- (2) 对于一对点 $x, y \in \mathbb{R}^d$，令 $v = x - y$，考虑设计一个随机映射 $g : \mathbb{R}^d \to \mathbb{R}$，使得 $\mathbb{E}[g^2(v)] = \|v\|^2$。
- (3) 通过某种方式多次试验“取平均值”，得到高概率的小偏差保证，再对所有点对 Union Bound 一下。

仿照 SimHash，取随机高斯向量 $w \in \mathbb{R}^d$（每一维都是服从 $\mathcal{N}(0, 1)$ 的随机变量），令 $g(v) = \langle v, w \rangle$，则：

$$
\mathbb{E}[g^2(v)] = \sum_{i = 1}^d v_i^2 \mathbb{E}[w_i^2] = \sum_{i = 1}^d v_i^2 = \|v\|^2
$$

由于随机高斯向量之和仍为随机高斯向量，不能直接“求和取平均值”。考虑取 $m$ 个独立的高斯向量 $w_1, \cdots, w_m$，令：

$$
f(v) = \frac{1}{\sqrt{m}} (\langle v, w_1 \rangle, \cdots, \langle v, w_m \rangle)
$$

则 $\mathbb{E}[f^2(v)] = \|v\|_2^2$。由 Chernoff Bound 可知：

$$
\forall v \in \mathbb{R}^d, \Pr[|f^2(v) - \|v\|^2| \geq \epsilon \|v\|^2] \leq \exp(-\epsilon^2 m)
$$

再对所有点对使用 Union Bound，要求 $\exp(-\epsilon^2 m) \cdot C_n^2 \leq \delta$，解得 $m = O \left( \dfrac{1}{\epsilon^2} \left( \log n + \log \dfrac{1}{\delta} \right) \right)$。

#### 性质：Data Oblivious

即无论输入数据集长成什么样，构造的方法都不变。上面的映射只依赖于参数 $m$ 和随机数 $w_i$。

这一性质适用于数据流、并行计算，因为只需储存 $m$ 个 $d$ 维的随机高斯向量，随后每个 $d$ 维点都可以直接求出降维后的 $m$ 维向量。

------

在实现中，$m$ 这个界往往相当松；同时，可以将 $w_i$ 用每维在 $\pm 1$ 中独立均匀随机的向量替代，即用离散的随机单位立方体顶点来近似连续的随机方向。

JL 变换确保的是**每一对**点之间的距离满足要求，若只需确保点对距离和（或者说，**平均**而言），也即点对差的 $2$-范数之和：

$$
\sum_{i = 1}^{C_n^2} \|f(x_i)\| \in (1 \pm \epsilon) \cdot \sum_{i = 1}^{C_n^2} \|x_i\|
$$

则可以去掉 $\log n$ 因子。

#### 应用：线性回归

给定 $\{(x_i, y_i)\}_{i = 1}^n \subset \mathbb{R}^d \times \mathbb{R}$，欲求 $w \in \mathbb{R}^d$，最小化 $\displaystyle\sum_{i = 1}^n (\langle x_i, w \rangle + w_0 - y_i)^2$。

方便起见，通过将常数项 $w_0$ 置于 $w$ 的第 $0$ 维、并给每个 $x_i$ 增加值为 $1$ 的第 $0$ 维，我们可以在问题描述中省去 $w_0$。下面的内容也将依此叙述。

通过将输入写成矩阵 $X = \begin{bmatrix} x_1 \\ \vdots \\ x_n \end{bmatrix} \in M_{n \times d}(\mathbb{R})$ 和向量 $y = \begin{bmatrix} y_1 \\ \vdots \\ y_n \end{bmatrix} \in \mathbb{R}^n$，我们的目标即求：

$$
\arg \min_{w \in \mathbb{R}^d} \|Xw - y\|^2
$$

对每个 $w_i$ 求偏导、令偏导数为 $0$，汇总得到的 $d$ 个方程可得：

$$
X^T Xw = X^T y
$$

当 $X^T X \in M_n(\mathbb{R})$ 可逆，即求得 $w = (X^T X)^{-1} X^T y$，时间复杂度为 $O(d^3 + nd^2)$。

- $X^T X$ 可逆（即 $\text{rank}(X^T X) = d$）当且仅当 $\text{rank}(X) = d$（证明：二者的零空间相等），也即 $X$ 的 $d$ 列线性无关。
- 下面给出 $X^T X$ 不可逆的两种解决方案：
- 解法一：对 $X$ 随机扰动，此时得到的矩阵大概率满秩。
- 解法二：只保留 $X$ 中线性无关的列，在此时算得的 $w$ 的基础上，在先前删去的位置补 $0$ 即可。

一般而言，对线性回归问题我们认为 $n \gg d$，故 $O(nd^2)$ 为时间复杂度的主项。

当 $n$ 较大，为优化时间复杂度，注意到我们所求的是差的 $2$-范数的最小值，欲应用 JL 变换 $x \mapsto Ax$，使得：

$$
\|AXw - Ay\|^2 \in (1 \pm \epsilon) \cdot \|Xw - y\|^2
$$

其中 $A \in M_{m \times n}(\mathbb{R})$，则预处理 $AX \in M_{m \times d}(\mathbb{R}), Ay \in \mathbb{R}^m$ 后对其暴力求解线性回归，便可在 $O(nmd + md^2)$ 的时间复杂度内得到一组相对误差不超过 $\epsilon$ 的近似解。

##### 子空间版的 JL 引理

想象自然是美好的，但问题在于：为确保所得解的相对误差不超过 $\epsilon$，我们要求**所有** $w \in \mathbb{R}^d$ 都满足上述相对误差限制，但我们显然无法对无穷多个向量应用 Union Bound。

注意到 $\{Xw - y \mid w \in \mathbb{R}^d\}$ 必然属于 $\mathbb{R}^n$ 的一个至多 $d + 1$ 维的**子空间**，而我们有下列结论：

- 设 $S \subset \mathbb{R}^n$ 为一个 $d$ 维子空间，则只需取 $m = O \left( \dfrac{1}{\epsilon^2} \left( d \log \dfrac{1}{\epsilon} + \log \dfrac{1}{\delta} \right) \right)$ 即可确保：

$$
\Pr[\forall v \in S, \|f(v)\| \in (1 \pm \epsilon) \cdot \|v\|^2] \geq 1 - \delta
$$

视 $\epsilon$ 为常数，则 $m = O(d)$，故时间复杂度为 $O(nd^2 + d^3)$。现在的瓶颈在于求解 $AX$。

##### Clarkson-Woodruff 变换

- Reference: [Low Rank Approximation and Regression in Input Sparsity Time](https://arxiv.org/pdf/1207.6365v4) (Clarkson & Woodruff, STOC 2013).

在现实应用中，$X$ 往往是稀疏的。记 $\text{nnz}(X)$ 表示 $X$ 中非零元素的数量，其满足 $\text{nnz}(X) \ll nd$，我们期待可以 $O(\text{nnz}(X))$ 地计算 $AX$。

考虑取 $A$ 为特殊矩阵 $PD$，其中：

- $P \in M_{m \times n}(\mathbb{R})$ 为随机 $01$ 阵，其每列恰有一个 $1$，且其行号均匀随机取自 $\{1, \cdots, m\}$。
- $D \in M_n(\mathbb{R})$ 为随机对角阵，其对角线上每个元素均匀随机取自 $\pm 1$。

则只需枚举 $X$ 的非零元素各一次即可求得 $AX$，且可以证明只需取 $m = \tilde{O} \left( \dfrac{d^2}{\epsilon^2} \right)$ 即可得到常数错误概率的保证。

视 $\epsilon$ 为常数，则 $m = \tilde{O}(d^2)$，故时间复杂度为 $O(\text{nnz}(X)) + \tilde{O}(d^4)$。

##### 快速 JL 变换 (FJLT)

- Reference: [The Fast Johnson–Lindenstrauss Transform and Approximate Nearest Neighbor](https://www.cs.princeton.edu/~chazelle/pubs/FJLT-sicomp09.pdf) (Ailon & Chazelle, SICOMP 2009).

回到点集 $P$ 上的情况。不妨设 $d$ 为 $2$ 的幂，考虑取 $A$ 为特殊矩阵 $PHD$，其中：

- $P \in M_{m \times d}(\mathbb{R})$ 中每个元素以 $q$ 的概率服从 $\mathcal{N} \left( 0, \dfrac{1}{q} \right)$、以 $1 - q$ 的概率取 $0$，其中 $q = \min \left\{ \Theta \left( \dfrac{\log^2 n}{d} \right), 1 \right\}$。
- $H \in M_d(\mathbb{R})$ 为 Hadamard 矩阵。
- $D \in M_d(\mathbb{R})$ 为随机对角阵，其对角线上每个元素均匀随机取自 $\pm 1$。

取 $m = O \left( \dfrac{\log n}{\epsilon^2} \right)$，可以证明：

$$
\Pr[\forall x, y \in P, \|Ax - Ay\| \in (1 \pm \epsilon) \cdot \|x - y\|] \geq \frac{2}{3}
$$

同时，$\forall x \in P$，$Ax$ 可在 $O(d \log d + qmd)$ 的时间复杂度内计算，故降维的总时间复杂度为 $O(nd \log d + nqmd) = O \left( nd \log d + \dfrac{n \log^3 n}{\epsilon^2} \right)$。

FJLT 同样有子空间版，视 $\epsilon$ 为常数时同样可以给出 $m = O(d)$。将其应用于 Clarkson-Woodruff 变换的结果中，将 $\tilde{O}(d^2)$ 维降至 $O(d)$ 维。此时只需对 $\tilde{O}(d^2)$ 个单位正交基变换即可，总时间复杂度为 $O(\text{nnz}(X)) + \tilde{O}(d^3)$。

##### 子空间版 JL 引理的证明

显见只需考虑单位模长的 $v$，考虑将单位球上的点**离散化**，确保这有限个点上的模长，再推及单位球上的其他点。

设 $\epsilon \in (0, 1)$，称单位球面上的有限集 $N$ 为 $\epsilon$-网，若：

- (1) $\forall v$ 在单位球面上，$\exists x \in N, \text{s.t. } \|v - x\| \leq \epsilon$。
- (2) $\forall x, y \in N, \|x - y\| \geq \epsilon$。

其存在性由如下贪心算法确保：

- 初始化 $N \leftarrow \varnothing$，$S$ 为单位球面上点的集合。
- 当 $S$ 非空，任取 $x \in S$，令 $N \leftarrow N \cup \{x\}, S \leftarrow S \backslash B(x, \epsilon)$；当 $S$ 为空则停止。
- 由于 $\forall x \in N, B \left( x, \dfrac{\epsilon}{2} \right)$ 两两不交，且均包含于 $B \left( 0, 1 + \dfrac{\epsilon}{2} \right)$ 中，据体积估算即得上述过程会在有限步内结束，且得到的 $|N| \leq \left( 1 + \dfrac{2}{\epsilon} \right)^d$。

得到 $N$ 后，考虑用 $\epsilon$-网表示单位球面上的其他点：

- 设 $v$ 在单位球面上，取 $x_0 \in N$ 使得 $\|v - x_0\| \leq \epsilon$。
- 当 $v \neq x_0$，令 $c_1 = \|v - x_0\| \leq \epsilon$，取 $x_1 \in N$ 使得 $\left\| \dfrac{v - x_0}{c_1} - x_1 \right\| \leq \epsilon$。
- 当 $v \neq x_0 + c_1 x_1$，依次类推，要么将 $v$ 表示为有限和 $x_0 + \displaystyle\sum_{i = 1}^k c_i x_i$。
- 又或者得到序列 $\{x_i\}_{i = 0}^{+\infty} \subset N, \{c_i\}_{i = 1}^{+\infty} \subset \mathbb{R}$，使得 $c_k \leq \epsilon^k$。
- 由构造过程可知 $\forall k \in \mathbb{N}, \left\| v - x_0 - \displaystyle\sum_{i = 1}^k c_i x_i \right\| \leq \epsilon^{k + 1}$，故 $\displaystyle\sum_{k = 1}^{+\infty} c_k x_k = v$。
- 总之，单位球面上的点均可表示为**至多可数**项 $N$ 中向量的线性组合。
- 对 $x_k$ 为 $N$ 中同一点的情况并项求和，即知 $v$ 可以表示为 $N$ 中向量的线性组合。

接下来通过 JL 变换保持 $N$ 中点的范数，降至 $O \left( \dfrac{d}{\epsilon^2} \log \dfrac{1}{\epsilon} \right)$ 维，再由三角不等式可知该变换的相对误差不超过 $\dfrac{2 \epsilon}{1 - \epsilon}$，调整一下常数即可。

### 基于维度间隐含相关性的降维：主成分分析 (principal component analysis, PCA)

给定 $n$ 个点 $x_1, \cdots, x_n \in \mathbb{R}^d$，再给定 $m < n$，欲找到 $m$ 个“基向量” $v_1, \cdots, v_m \in \mathbb{R}^d$，使得：

$$
x_i = \sum_{j = 1}^m a_{i, j} v_j
$$

近似成立。现在我们尝试来衡量怎样的基构成了“最佳”的近似。

------

首先对数据进行标准化：

- (1) **均值归零**：设数据点均值 $\bar{x} = \dfrac{1}{n} \displaystyle\sum_{i = 1}^n x_i$，令 $x_i \leftarrow x_i - \bar{x}$。最后求得基后再把 $\bar{x}$ 加上去即可。
- (2) **维度归一**：数据的每一维可能代表不同的标度，考虑将其 $2$-范数归一，设 $d_j = \sqrt{\displaystyle\sum_{i = 1}^n x_{i, j}^2}$，令 $x_{i, j} \leftarrow \dfrac{x_{i, j}}{d_j}$。最后求得基后再把 $d_j$ 乘上去即可。

假设我们已经找到了一组基，令 $S = \text{span}(v_1, \cdots, v_m)$，我们期待 $x$ 到 $S$ 平面上一点的距离**尽可能小**，又或者由勾股定理、$x$ 在 $S$ 上的**投影长度** $\text{proj}(x, S)$ **尽可能大**。

方便起见，取 $S$ 的一组**标准正交基** (orthonormal basis) $v_1, \cdots, v_m$，即满足：

$$
\langle v_i, v_j \rangle = \delta_{i, j}
$$

则 $\text{proj}(x, S)^2 = \displaystyle\sum_{i = 1}^m \langle x, v_i \rangle^2$。

总之，我们的目标为：

- 求标准正交的向量 $v_1, \cdots, v_m \in \mathbb{R}^d$，最大化：

$$
\frac{1}{n} \sum_{i = 1}^n \sum_{j = 1}^m \langle x_i, v_j \rangle^2
$$

最优的这 $m$ 个向量称为 $x_1, \cdots, x_n$ 的前 $m$ 个主成分 (principal components)。

#### 特殊情况：$m = 1$

此时即求单位向量 $v$，最大化 $\dfrac{1}{n} \displaystyle\sum_{i = 1}^n \langle x_i, v \rangle^2$。

设 $X \in M_{n \times d}$ 将每个 $x_i$ 作为一行的矩阵，则所求即 $\|Xv\|^2 = v^T X^T Xv$。

令 $A = X^T X$，则目标等价于最大化 $v^T Av$。

- 这里 $A_{i, j}$ 即 $X$ 的第 $i, j$ 列的内积，可以用来描述某些相关性。
- 例如：$x_{p, q}$ 表示单词 $q$ 是否在文档 $p$ 中出现，则 $A_{i, j}$ 即单词 $i, j$ 共同出现的文档数量。

当 $A$ 为对角阵 $\text{diag}(\lambda_1, \cdots, \lambda_d)$，$v^T Av = \displaystyle\sum_{i = 1}^d \lambda_i v_i^2$，此时自然应令最大的 $\lambda_i$ 对应的 $v_i = 1$、其余 $v_j = 0$，也即令 $v = e_i$。

一般地，由于形如 $X^T X$ 的矩阵必可正交相似对角化，且其特征值非负，即存在正交矩阵 $Q$ 使得 $A = QDQ^T$，此时目标等价于最大化 $(v^T Q) D (Q^T v)$。求出最优的 $Q^T v = e_i$ 后，取 $v = Qe_i$ 即可，此即 $Q$ 的第 $i$ 列。

#### 一般情况：任意的 $m$

此时依次取前 $m$ 大的 $\lambda_i$ 对应的 $Q$ 的第 $i$ 列即可，也就是 $A$ 的前 $m$ 大特征值对应的**特征向量**。

------

直接求解所有主成分的时间复杂度是 $O(nd^2)$ 的，下面给出一种求解最大主成分的近似算法。

#### 幂法 (power iteration)

任取向量 $u$，只要不与上面给出的最大主成分 $v$ 垂直，则 $Au$ 将在 $v$ 方向上被拉伸。

只要最大特征值确实比次大特征值“明显大一些”，我们感觉拉伸充分多次后 $u$ 就将充分贴近 $v$ 的方向。

考虑随机选取方向向量 $u_0$（每维取自正态分布 $\mathcal{N}(0, 1)$ 再归一化），迭代令 $u_i = \dfrac{Au_{i - 1}}{\|Au_{i - 1}\|}$，当 $u_i, u_{i - 1}$ “充分接近”就认为 $u_i$ 已经“充分接近” $v$ 的方向，返回之。单次迭代的时间复杂度为 $O(\text{nnz}(X))$。

------

下面说明上述算法的正确性，并给出迭代次数的上界。

设 $\lambda_i$ 为第 $i$ 大特征值，$v_i$ 为第 $i$ 大主成分，由于 $A^j = QD^j Q^T$，其第 $i$ 大特征值为 $\lambda_i^j$。

将初始方向向量表出为 $u_0 = \displaystyle\sum_{i = 1}^d \langle u_0, v_i \rangle v_i$。归一化之前的 $u$ 满足 $\langle u_0, v_1 \rangle \sim \mathcal{N}(0, 1)$，由正态分布的性质可知 $P(|\langle u, v_1 \rangle| \geq 1) = \Omega(1)$。而归一化因子 $\|u\|$ 的平方服从 $\chi^2$ 分布，故开方后大概率接近 $\sqrt{d}$。总之 $|\langle u_0, v_1 \rangle| = \Omega \left( \dfrac{1}{\sqrt{d}} \right)$ 以 $\Omega(1)$ 概率成立。

接下来考虑 $\left\langle \dfrac{A^j u_0}{\|A^j u_0\|}, v_1 \right\rangle$，一方面 $\langle A^j u_0, v_1 \rangle = |\langle u_0, v_1 \rangle| \lambda_1^j$，另一方面：

$$
\begin{aligned}
\|A^j u_0\|^2 &= \sum_{i = 1}^d (\langle u_0, v_i \rangle \lambda_i^j)^2 \\
&\leq \langle u_0, v_1 \rangle^2 \lambda_1^{2j} + \sum_{i = 2}^d \langle u_0, v_i \rangle^2 \cdot \lambda_2^{2j} \\
&\leq \langle u_0, v_1 \rangle^2 \lambda_1^{2j} + \lambda_2^{2j}
\end{aligned}
$$

故有：

$$
\begin{aligned}
\left\langle \frac{A^j u_0}{\|A^j u_0\|}, v_1 \right\rangle &= \frac{\langle A^j u_0, v_1 \rangle}{\|A^j u_0\|} \\
&\geq \frac{|\langle u_0, v_1 \rangle| \lambda_1^j}{\sqrt{\langle u_0, v_1 \rangle^2 \lambda_1^{2j} + \lambda_2^{2j}}} \\
&\geq \frac{|\langle u_0, v_1 \rangle| \lambda_1^j}{|\langle u_0, v_1 \rangle| \lambda_1^j + \lambda_2^j} \\
&\geq 1 - \frac{\lambda_2^j}{|\langle u_0, v_1 \rangle| \lambda_1^j} \\
&= 1 - \left( \frac{\lambda_2}{\lambda_1} \right)^j \Omega(\sqrt{d})
\end{aligned}
$$

记 $k = \dfrac{\lambda_1}{\lambda_2} \geq 1$（称为 spectral gap），当 $k > 1$，则为了使投影达到 $1 - \epsilon$，只需进行 $O \left( \log_k \left( \dfrac{d}{\epsilon} \right) \right)$ 次迭代。

------

如需计算第 $m$ 大主成分，考虑递归：

- 对每个数据点 $x_i$ 删去在 $v_1$ 上的成分，即 $x_i \leftarrow x_i - \langle x_i, v_1 \rangle v_1$。
- 在新数据集上施以幂法，求得的最大主成分就是原数据集的次大主成分的近似。
- 依次递归下去即可求得第 $m$ 大主成分。

但这样可能会破坏 $X$ 的**稀疏性**。以 $m = 2$ 为例，考虑隐式地修改数据点，即 $u_0$ 从垂直于 $v_1$ 的 $d - 1$ 维单位球面的部分均匀采样，只需令 $u_0 \leftarrow u_0 - \langle u_0, v_1 \rangle v_1$ 即可。

------

若 $\lambda_1 = \cdots = \lambda_i > \lambda_{i + 1}$，幂法输出的向量 $\tilde{v}_1$ 将近似在 $\text{span}(v_1, \cdots, v_i)$ 中，此时应期待 $\tilde{v}_1$ 在 $\text{span}(v_1, \cdots, v_i)$ 上的**投影**尽可能大，此时给出的保证就是关于 $\dfrac{\lambda_1}{\lambda_{i + 1}}$ 的。

### 低秩近似：基于奇异值分解 (SVD)

将矩阵 $A \in M_{n \times d}(\mathbb{R})$ 分解为 $U \Sigma V^T$，其中 $U \in M_n(\mathbb{R}), \Sigma \in M_{n \times d}(\mathbb{R}), V \in M_d(\mathbb{R})$，$U, V$ 正交，$\Sigma$ 只有主对角线上的前 $r = \text{rank}(A)$ 个值非零，其为 $A$ 的奇异值 $s_1 \geq \cdots \geq s_r > 0$。

设 $U_k \in M_{n \times k}(\mathbb{R}), V_k \in M_{d \times k}(\mathbb{R})$ 分别为 $U, V$ 取前 $k$ 列所得，$\Sigma_k \in M_k(\mathbb{R})$ 为 $\Sigma$ 取前 $k$ 行、列所得，则 $A_k = U_k \Sigma_k V_k^T$ 的秩不超过 $k$。

当 $k$ 很小，一方面，储存 $A_k$ 的代价仅为 $k(n + d)$，远小于储存 $A$ 的 $nd$；另一方面，$A_k$ 也可以快速地参与矩阵乘法，其与 $x \in \mathbb{R}^d$ 相乘的时间复杂度也仅为 $O(k(n + d))$，远低于直接与 $A$ 相乘的 $O(nd)$。

我们自然期待 $A_k$ 可以以“较好的精度“近似 $A$，下面的定理指出其甚至是最优的。

#### Eckart-Young-Mirsky 定理

内容：设 $A \in M_{n \times d}(\mathbb{R})$，则 $\forall 1 \leq k \leq \text{rank}(A)$，有：

$$
\forall B \in M_{n \times d}(\mathbb{R}), \text{rank}(B) = k \Rightarrow \|A - A_k\|_F \leq \|A - B\|_F
$$

其中 $\|\cdot\|_F$ 为 Frobenius 范数。

直觉上，由于 $A_k = \displaystyle\sum_{i = 1}^k s_i (u_i v_i^T)$，正交性决定 $\|u_i v_i^T\|_F = 1$，而 $s_i$ 决定了每一项 $u_i v_i^T$ 对矩阵贡献的“显著性”，而 $A_k$ 通过取最显著的 $k$ 项，就得到了某种意义下最好的近似。

#### 应用：SVD $\Leftrightarrow$ PCA

设 $X$ 的 SVD 为 $X = U \Sigma V^T$，则 $A = X^T X = V \Sigma^2 V^T$，故 $V$ 的列即为 $X$ 的主成分。

因此，通过求解 SVD 可以求解 PCA，通过求解 PCA 可以求解 SVD 中的 $V$。

------

但 SVD 也有一些局限性：

- 即便输入的 $A$ 稀疏，SVD 所得的 $U, V$ 也是稠密的。
- 同时，$U, V$ 与原矩阵行列的关系不显著，可解释性不佳。

因此，还有 CUR 分解等用于解决上述问题的低秩分解方法。
