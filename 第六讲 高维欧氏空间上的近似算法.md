下面讨论的问题中假定输入了 $n$ 个 $d = O(\log n)$ 维空间中、每维坐标均在 $[\Delta]$ 中的点集 $P$。

如无特殊说明，下面涉及的范数默认为 $2$-范数。

### 随机平移四分树

考虑选取随机向量 $v \in [0, \Delta]^d$，考虑对区域 $([-v_i, 2 \Delta - v_i])_{i = 1}^d$ 建树（等价地，将 $P$ 中的点沿向量 $v$ 平移），得到一棵随机平移后的四分树。

方便起见，下设四分树的每个超立方体的边长都是 $2$ 的幂。

#### 性质：“短边”被“大超立方体”分开的概率“较低”

内容：设 $x, y \in [\Delta]^d$，取随机平移四分树的一个包含 $x$ 边长为 $2^k$ 的超立方体 $S$，则：

$$
\Pr[y \not\in S] \leq \frac{\sqrt{d} \cdot ||x - y||}{2^k}
$$

证明：

$$
\begin{aligned}
\Pr[y \not\in S] &= \Pr[\exists 1 \leq i \leq d, \text{s.t. } x, y \text{ 在第 } i \text{ 维被“切开”}] \\
&\leq \sum_{i = 1}^d \Pr[x, y \text{ 在第 } i \text{ 维被“切开”}] &\quad (\text{Union Bound}) \\
&= \sum_{i = 1}^d \frac{|x_i - y_i|}{2^k} \\
&\leq \frac{\sqrt{d} \cdot ||x - y||}{2^k} &\quad (\text{均值不等式})
\end{aligned}
$$

#### 树嵌入 (tree embedding)

考虑将 $P$ 嵌入到带权树 $T = (V, E)$ 上，其中 $P \subset V$。我们期待用树上距离“代替”欧氏距离，如此便有机会使用树上的高效算法获得近似解。首先要求：

$$
\forall x, y \in P, \text{dis}_T(x, y) \geq ||x - y||
$$

为了衡量这一嵌入的优劣，引入失真比 (distortion)：

$$
\max_{x, y \in P, x \neq y} \frac{\text{dis}_T(x, y)}{||x - y||} \in [1, +\infty)
$$

我们期待其尽可能小。

------

为确保树的结果规整，一般要求这棵树是分层分离树 (2-HST, 2-hierarchically separated tree)：

- (1) $P$ 构成 $V$ 的叶子集合。
- (2) 所以叶子的深度相同。
- (3) 从叶子到根的路径上，边权依次乘以 $2$。

考虑先构造随机平移四分树，对每个边长为 $2^k$ 的超立方体对应的节点，令其到子节点的边权为 $\sqrt{d} \cdot 2^k$，此即超立方体的直径。

首先由 LCA 层向下的边权易见 $\forall x, y \in P, \text{dis}_T(x, y) \geq ||x - y||$ 成立。

设树高 $h = \log_2 \Delta$，接下来考虑距离的期望上界：

$$
\begin{aligned}
\mathbb{E}[\text{dis}_T(x, y)] &= \sum_{i = 1}^h \Pr[\text{LCA}(x, y) \text{ 在自底向上第 } i \text{ 层}] \cdot 2 \sum_{j = 1}^i \sqrt{d} \cdot 2^j \\
&\leq \sqrt{d} \cdot \sum_{i = 1}^h \Pr[x, y \text{ 分居两个边长为 } 2^{i - 1} \text{ 的超立方体中}] \cdot 2^{i + 2} \\
&\leq \sqrt{d} \cdot \sum_{i = 1}^h \frac{\sqrt{d} \cdot ||x - y||}{2^{i - 1}} \cdot 2^{i + 2} \\
&= 8dh ||x - y||
\end{aligned}
$$

故对于确定的点对 $x, y$ 而言，失真比**期望**是 $O(dh)$ 的。

------

尽管这一算法的失真比（即便！）在期望下都达不到常数级别，但其有一些明显的优势：

- (1) 可以不显式建树，以解决数据流上的问题等。
- (2) 它能保持**每对**点的期望距离，故广泛适用于目标函数是距离和的问题。
- (3) 它的时间复杂度关于 $d$ 是线性的，适用高维情况。

##### 应用：近似欧氏最小生成树

首先对 $P$ 做树嵌入得到 $T$，然后求解 $T$ 上 $P$ 的 MST（也即保留其虚树；**注意这棵树上包含 $P$ 中没有的点，不能直接作为所求！**）得到边集 $E$。

$P$ 的真实 MST 边集为 $E^*$，则：

$$
\mathbb{E}[w(E)] \leq \sum_{(x, y) \in E^*} \mathbb{E}[\text{dis}_T(x, y)] \leq O(dh) \cdot \sum_{(x, y) \in E^*} ||x - y|| \leq O(dh) \cdot \mathbb{E}[w(E^*)]
$$

- 可以发现这个界是比较松的，因为 $\displaystyle\sum_{(x, y) \in E^*} \mathbb{E}[\text{dis}_T(x, y)]$ 很可能会将同一条边累加若干次。

最后考虑将 $E$ 转化回 $P$ 的 MST：考虑其先序遍历即可。得到的解是期望 $O(dh)$-近似的。

##### 应用：近似欧氏最小权完美匹配

设有 $2n$ 个 $[\Delta]^d$ 上的点集 $P$，欲在 $n$ 个点对间连边，使得每个点恰好参与到其中一条边中。

还是考虑对 $P$ 做树嵌入得到 $T$，此时在每个非叶子节点贪心即可：

- 若一个子树内有奇数个叶子，则其中必有一个叶子需要与子树外的叶子匹配。

得到的解仍是期望 $O(dh)$-近似的。

#### 高维 spanner

令 $m = O(\log n)$，考虑独立随机对点集构建 $m$ 个随机平移四分树，对于其中每一棵树：

- 对于树上的每个非叶子节点，任取其子树内一点作为中心构造菊花图。

易见得到的 spanner 边集 $E'$ 的大小是 $O(nh \log n)$ 的，下面分析其近似比。

------

考虑边长为 $2^k$ 的超立方体对应节点所构造的菊花图，注意到其直径不超过 $2 \sqrt{d} \cdot 2^k$。

对于两点 $x, y$，利用一开始的性质，我们期待其同时处于某一**特定大小**的超立方体的概率是 $\Omega(1)$ 的，如此便可通过独立随机的 $m$ 棵树达到 $\dfrac{1}{\text{poly}(n)}$ 的失败概率。

取 $k = \lceil \log_2 (\sqrt{d} \cdot ||x - y||) \lceil + 1$，则 $\dfrac{\sqrt{d} \cdot ||x - y||}{2^k} \leq \dfrac{1}{2}$，即单次成功概率 $\geq \dfrac{1}{2}$，此时有：

$$
\text{dis}_{E'}(x, y) \leq 2 \sqrt{d} \cdot 2^k = O(d) \cdot ||x - y||
$$

故该算法能以 $\dfrac{1}{\text{poly}(n)}$ 的失败概率给出一个 $O(d)$-spanner。

##### 应用：（近似比更好的！）近似欧氏最小生成树

在上述 spanner 上求 MST 即可。得到的解是 $O(d)$-近似的。

### 随机平移网格

#### 邻域查询

设有 $n$ 个 $[\Delta]^d$ 上的点集 $P$，给定 $r > 0$，进行若干次查询，每次给定 $q \in [\Delta]^d$，我们想要高效地求出 $|B(q, r) \cap P|$ 的近似值。

- 或者更一般地，求出 $f(B(q, r) \cap P)$ 的近似值，其中 $f$ 是对集合的无交并“可加”的。

考虑这样的 $(1 + \alpha)$-近似，即要求实际返回的 $c$ 满足：

$$
|B(q, r) \cap P| \leq c \leq |B(q, (1 + \alpha) r) \cap P|
$$

------

令 $l = \dfrac{\alpha r}{\sqrt{d}}$，选取随机向量 $v \in [0, l]^d$，将各点沿向量 $v$ 平移，找出所有与 $B(q, r)$ 有交的网格作为集合 $Q$，并将 $Q$ 中各网格的点数求和作为估计。

显见 $B(q, r)$ 中的点都被计入，且被计入的点均在 $B(q, (1 + \alpha) r)$ 中，满足近似要求。

可以证明 $|Q| = O \left( 2^{d \alpha^{-\frac{2}{3}}} \text{poly}(d) \right)$，下面考虑如何快速求出集合 $Q$。

$l$ 的选取决定了满足条件的网格的每一维与 $q$ 所在的网格要么位于同一区间中、要么位于相邻区间中。考虑 dfs，枚举每一维是同一区间、左侧相邻区间还是右侧相邻区间，并在搜索过程中对网格距离超过 $r$ 的情况剪枝。

由于每一个被枚举到但被剪枝的网格必有一个相邻网格，使之在 $Q$ 中，则这样的网格数量是 $O(d |Q|)$ 的，故上述 dfs 搜索过的总网格数量也是 $O(d |Q|)$ 的。

对网格用 Trie 套 map 记录其中包含的 $P$ 中点的数量，查询时在 Trie 上移动指针即可。

时间复杂度为 $O(nd \log n) \sim O \left( 2^{d \alpha^{-\frac{2}{3}}} \text{poly}(d) \log n \right)$。
